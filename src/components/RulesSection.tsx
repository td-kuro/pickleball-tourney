// Placeholder for future tournament rule configuration
// (e.g. scoring format, number of courts, pairing method).
export function RulesSection() {
  return (
    <section className="card placeholder-section">
      <h2>Tournament Rules</h2>
      <p>Rule configuration will be added here in a future update.</p>
    </section>
  );
}
